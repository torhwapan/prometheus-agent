# Prometheus Agent V3 Prompts

- `intent_extraction.md`: Replaces the V2 intent skill. Use it to extract request parameters before creating an inspection.

V3 reuses V2 prompts for later AI steps:

- `prompts_v2/metric_ai_comment.md`: Per-metric range sample supplemental analysis.
- `prompts_v2/correlation_analysis.md`: Cross-metric correlation analysis.
