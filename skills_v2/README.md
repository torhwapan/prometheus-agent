# Prometheus Agent V2 Skills

- `intent-extraction`: Extract `prometheus_url`, `job`, `instance`, and query window from chat.
- `metric-query-catalog`: Explain configured job families and metric query behavior.
- `analysis-explanation`: Constrain AI explanations and cross-metric correlation to tool evidence.

These skills are designed for the v2 HTTP service in `prometheus_agent_v2/`.
